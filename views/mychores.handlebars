<!DOCTYPE html>
<html>
  <head>
    <title>My Chores</title>
    {{> styles }}
    <link rel="stylesheet" type="text/css" href="css/fluid.css">
  </head>
  <body>
	<!-- this is a comment in HTML -->
  {{> navbar }}
	<div class="container-fluid">
    <div class="jumbotron">
      <h1>My Chores</h1>
      {{#unless weekoff }}
        {{#if chores }}
        <p>Hi, {{ username }}. You have {{ chores.length }} unfinished chores this week. Get to it!</p>
        {{else}}
        <p>Hi, {{ username }}. Looks like you've finished all your chores this week!</p>
        {{/if}}
      {{else}}
        <p>
          No responsibilites for you this week, {{ username }}! Go to the beach or something. :D
        </p>
      {{/unless}}
    </div>

{{!--     {{#unless chores}}
    <form action="/deal" method="POST">
      <div class="row">
        <div class="col-xs-12">
          <button class="center-block btn btn-lg btn-info" type="submit">Deal</button>
        </div>
      </div>
    </form>
    {{/unless}} --}}



    <div id="myCarousel" class="carousel slide">
      <!-- remove comments if we want the carousel dots on the bottom -->
      <!--<ol class="carousel-indicators">
        {{#each chores }}
          {{#if @first}}
            <li data-target="#myCarousel" class="active"></li>
          {{else}}
            <li data-target="#myCarousel"></li>
          {{/if}}
        {{/each}}
      </ol> -->
      <div class="carousel-inner" role="listbox">
        {{#each chores }}
          {{#if @first}}
            <div class="item active">
              {{> flippable-card-with-buttons }}
            </div>
            {{else}}
               <div class="item">
              {{> flippable-card-with-buttons }}
            </div>
            {{/if}}
        {{/each}}
      </div>
      <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"  style="color:black"></span>
        <span class="sr-only">Previous</span>
      </a>
      <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"  style="color:black"></span>
        <span class="sr-only">Next</span>
      </a>
    </div>


    {{> footer }}

    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://code.jquery.com/jquery.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="/js/bootstrap.min.js"></script>
    <script src="/js/card-util.js"></script>
  </body>
</html>
